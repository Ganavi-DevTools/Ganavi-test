name: sequential jobs
on: 
    push:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:  
            - name: Install the cowsay
              run: sudo apt-get install cowsay -y

            - name: execute cowsay cmd
              run: cowsay -f dog "Run for cover, I am a dog Woof..." >> dog.txt

            - name: Sleep for 10 seconds
              run: sleep 10

    test:
        needs: build
        runs-on: Ubuntu-latest
        steps:
            - name: Sleep for 10 seconds
              run: sleep 10

            - name: Test file exists
              run: grep -i "dog" dog.txt
    deploy: 
        needs: test
        runs-on: ubuntu-latest
        steps:         
            - name: Read the file
              run: cat dog.txt

            - name: deploy
              run: echo "Deploying..."
