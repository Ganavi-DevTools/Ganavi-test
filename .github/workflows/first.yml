name: First workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
            - name: Checkout code
              uses: actions/checkout@v4

            - name: execute cowsay cmd
              run: cowsay -f dragon "Run for cover, I am a Dragon Rawr..." 

            - name: first_step
              run: |
               echo "Running the first step"
               ls
               cat demo.txt
