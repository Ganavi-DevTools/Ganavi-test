name: Exploring Variables and Secrets
on:
  push:
env:
  DOCKER_USERNAME: ganavirpatel
  IMAGE_NAME: github-actions-nginx
  CONTAINER_REGISTRY: docker.io  
 
jobs:  
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Docker Build
        run: docker build -t $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest 
        
      - name: Docker Login
        env:
          DOCKER_PASSWORD: Ganavi@RaniRamesh12
        run: docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD

      - name: Docker Publish
        run: docker push $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest
 
  deploy:
    needs: docker
    runs-on: ubuntu-latest
    steps:
      - name: Docker Run
        run: docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

 ####################env variables on step level###################################
#jobs:
#   docker:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Docker Build
#        env:
#            DOCKER_USERNAME: ganavirpatel
#            IMAGE_NAME: github-actions-nginx
#            CONTAINER_REGISTRY: docker.io     
#        run: docker build -t ${{ env.CONTAINER_REGISTRY }}/$DOCKER_USERNAME/$IMAGE_NAME:latest 

#      - name: Docker Login
#        env:
#          DOCKER_PASSWORD: Ganavi@RaniRamesh12
#          DOCKER_USERNAME: ganavirpatel 
#        run: docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD
#      - name: Docker Publish
#        env:
#            DOCKER_USERNAME: ganavirpatel
#            IMAGE_NAME: github-actions-nginx
#            CONTAINER_REGISTRY: docker.io 
#        run: docker push $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest
 
#  deploy:
#    needs: docker
#    runs-on: ubuntu-latest
#    steps:
#      - name: Docker Run
#        env: 
#            DOCKER_USERNAME: ganavirpatel
#            IMAGE_NAME: github-actions-nginx
#            CONTAINER_REGISTRY: docker.io 
#        run: docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest